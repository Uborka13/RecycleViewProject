<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="test.soft.ubi.recycleviewproject">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.gms.vision.DEPENDENCIES"
            android:value="barcode" />

        <activity android:name=".mashedup.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mashedup.activities.ListActivity" />
        <activity android:name=".mashedup.activities.DetailsActivity" />
        <activity android:name=".mashedup.activities.QRReaderActivity" />
        <activity
            android:name=".mashedup.activities.ScanActivity"
            android:launchMode="singleTask" />
        <activity android:name=".recycleviewsample.activity.ListHolderActivity"></activity>
    </application>

</manifest>